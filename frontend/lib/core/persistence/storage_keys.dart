/// Shared SecureStorage keys for AOD settings, kept in one place so the
/// settings datasource and any future callers can't drift apart.
const kAodEnabledKey = 'aod_enabled';
const kUse12HourClockKey = 'use_12_hour_clock';
const kPixelShiftEnabledKey = 'pixel_shift_enabled';
const kBrightnessLevelKey = 'brightness_level';
const kOnboardingCompleteKey = 'onboarding_complete';
